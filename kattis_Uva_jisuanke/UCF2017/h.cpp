#include <bits/stdc++.h>
using namespace std;
#define rep(i,j,k) for(int i = (int)j;i <= (int)k;i ++)
#define debug(x) cerr<<#x<<":"<<x<<endl
const int maxn=(int)1e6+5;

int main(){
	ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
	int t;cin>>t;
	while(t--){
		int n;cin>>n;
		rep(i,1,n)
		rep(i,1,n)
	}
	cin>>n;
	rep(i)
}
/* 

H:100个数，问最多可以选几个数字，组成长度大于等于k的区间不重叠的的上升子序列。
输出k=1 to n的答案
想反了
 */